# OS

## The ```os``` library

```rs
import os
```

Includes these utilities:

* ```os.name()```, returns the current os name, either ```windows```, ```macos``` or ```linux```
* ```os.command()```, runs the given command to the terminal
* ```os.clear()```, cleans the terminal
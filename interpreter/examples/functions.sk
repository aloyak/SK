fn fibonacci(n, previous = 0, current = 1) {
    if n > 0 {
        print(current)
        fibonacci(n - 1, current, previous + current)
    } 
}

fibonacci(10)

fn addUp(n = num(input("n: "))) {
    n * (n + 1) / 2 // No need to use 'return'
}

let result = addUp(50)
print(result)
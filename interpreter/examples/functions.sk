fn fibonacci(n, previous, current) {
    if n > 0 {
        print(current)
        fibonacci(n - 1, current, previous + current)
    } 
}

fibonacci(10, 0, 1)

fn addUp(n) {
    n * (n + 1) / 2 // No need to use 'return'
}

let result = addUp(50)
print(result)
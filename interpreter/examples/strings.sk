import string as str

let text = "¡Hello Flavortown!"

text = str.trim(text)
text = str.toUpper(text)
text = str.replace(text, "", " ")
text = str.split(text)

for i in [0..text.len() - 1] {
    for k in [0..i] {
        write(".")
    }
    print(text[i])
}


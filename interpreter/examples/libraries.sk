import math

// Please see "./simplelib.sk" for the rest of the example
import "simplelib.sk" as mylib

mylib.fibonacci(10)

print("Pi: " + str(math.PI)) // PI from the math lib inside the simplelib library

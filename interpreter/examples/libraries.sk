import math
import "simplelib.sk" // Please see "./simplelib.sk" for the rest of the example

simplelib.fibonacci(10)

print("Pi: " + str(math.PI)) // PI from the math lib inside the simplelib library
